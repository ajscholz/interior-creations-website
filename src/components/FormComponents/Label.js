import styled from "styled-components"

export default styled.label`
  display: block;
  font-size: 0.68rem;
  font-weight: 600;
  color: var(--lightgray);
  margin-left: 0;

  @media (min-width: 350px) {
    font-size: 0.75rem;
    margin-left: 0.75rem;
  }
`
